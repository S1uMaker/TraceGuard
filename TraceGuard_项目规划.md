# TraceGuard——基于 eBPF 的容器运行时安全监控与异常检测系统

## 一、项目定位与完成原则

开发一个在单机 Linux 环境中运行的容器安全监控系统，采集指定行为、识别来源容器、匹配检测规则并记录告警。

项目服务于实习简历和面试展示。目标是在 Codex 辅助下参与实现、调试和验证，做到主要功能能运行、演示可重复、关键代码基本能讲清楚。允许参考和复用现有实现，功能创新、行业领先性能和生产级完整性不作为要求。

本文保持方向性约束，具体接口、模块划分和实现方法在推进中确定。用户后续明确调整目标时，同步修订本文；不靠不断增加功能来定义项目完成。

### 2026-09-20 首版落地范围

按本轮要求直接交付 v0.1.0 源码，在 Windows 宿主机不进行编译或测试，Ubuntu 24.04 的运行验收单独保留。第一版选择成功 exec 行为：C/eBPF 挂载 `sched/sched_process_exec`，Go 读取 ring buffer，通过 cgroup v2 与 Docker 元数据关联来源，再执行可配置规则并追加保存 JSONL。默认提供 shell 与身份/系统信息查询两条规则、运行前检查和明确标识的离线回放。

运行范围具体收敛为 64 位 Ubuntu 虚拟机、cgroup v2、本机 rootful Docker；采集器直接运行在虚拟机中。CLI 和本地文件为正式首版入口。已实现、未验证的能力及下一步验收统一记录在 [PROGRESS.md](PROGRESS.md)，启动入口见 [README.md](README.md)。下文的最终完成标准仍要求实际证据，不因首版源码交付而自动勾选。

### 2026-09-21 首轮验收后的轻量完善

在已经完成的 v0.1.0 Ubuntu 初测基础上，按用户要求参考开源项目完善 v0.2.0：逐规则容器例外、unknown／告警／例外分类统计、可选终端文本输出。保持单机 C/eBPF + Go、JSON 规则和 JSONL 存储，不扩展探针或引入额外服务；具体来源见 [开源参考说明](docs/OPEN_SOURCE.md)。

本轮仅修改源码和相关说明，用户明确暂时不需要测试，因此未重新构建、运行或连接虚拟机。新功能状态与此前验收结论分开记录在 [PROGRESS.md](PROGRESS.md)，原验证证据保持不变。

## 二、最小完成版本与范围

2026-09-21 更新：已按用户后续授权在 Ubuntu 虚拟机完成构建、单元测试、竞态检测、静态检查、回放和真实容器验收。证据与排查过程见 [验证报告](docs/validation/2026-09-21/REPORT.md)。上节保留首版交付时的历史状态；用户讲解与简历准备仍需单独完成，不自动视为验收通过。

| 必需能力 | 完成时应具备的表现 |
| --- | --- |
| 行为采集 | 通过 eBPF 采集至少一种选定的真实行为，能够解释事件含义 |
| 来源识别 | 将目标事件关联到对应容器和进程；不能识别的情况明确标记 |
| 规则检测 | 实现至少一条有明确含义的检测规则，说明触发条件 |
| 告警与记录 | 输出事件、来源、规则和判断依据，保存可回看的结果 |
| 可重复运行 | 有基本启动、停止、配置和错误提示，能够按说明演示 |

运行范围限定为一台 Linux 主机或虚拟机及其中的 Docker 测试容器。行为类型先从进程、文件或网络中选择一种，具体选择以实现难度和可验证性为依据。

异常检测采用规则判断，告警表示满足可疑条件，不能直接等同于确认攻击。命令行和本地文件即可满足第一版展示需求。

更多行为类型、复杂查询和可视化均为可选扩展。多节点 Kubernetes、机器学习检测和自动阻断不属于本版完成要求。

## 三、技术路线与待定事项

采用 **C + eBPF 负责内核态采集，Go 负责用户态主程序，Python 按需辅助** 的技术路线。Go 开发经验通过实际监控与检测逻辑积累。

| 部分 | 技术 | 主要职责 |
| --- | --- | --- |
| 内核态采集 | C + eBPF | 捕获选定的真实行为，向用户态提供事件 |
| 用户态主程序 | Go | 加载与管理 eBPF 程序、读取事件、关联容器、匹配规则、记录告警，以及处理配置和启停 |
| 辅助脚本 | Python，可选 | 生成测试行为、整理和核对结果；按实际需要使用 |

- 从第一版采集流程开始，让 Go 承担用户态核心逻辑，逐步补齐必要的语言基础。
- 以实际可访问的目标环境为准，先核实内核、容器、权限和工具条件，不从规划推断兼容性。
- eBPF 工具库与构建方式、采集位置、事件传输、容器关联和存储方案，在相应阶段验证后选择。
- Go 的模块组织、错误处理和资源释放随功能逐步完善；并发机制按实际需要引入，不预设复杂架构。
- 有效且容易理解的实现可以保留，避免仅为增加技术名词而换框架或重写模块。

加入 Go 后，最小功能范围仍按第二节执行。语言学习与小功能实现同步推进，不额外增加微服务、消息队列或 Web 后台作为完成要求。

每项关键选择只需留下简短记录：选择什么、为什么、依据是什么、已知限制是什么。尚未验证的建议明确标为待验证，不写成已确定事实。

## 四、阶段目标与验收

| 阶段 | 推进内容 | 验收依据 |
| --- | --- | --- |
| 1. 基本采集 | 核实环境，选定一种行为，通过 Go 主程序加载 eBPF 程序并读取、输出事件 | 触发真实行为后收到对应事件，能解释 C 采集程序与 Go 主程序之间的数据传递 |
| 2. 最小完整流程 | 在 Go 主程序中加入容器识别、规则判断和告警 | 应触发与不应触发的场景都有结果，能核对事件来源和规则依据 |
| 3. 可重复运行 | 完善构建与运行说明、记录、配置、基本异常处理、资源释放和必要测试 | 按说明可构建并重复运行核心流程，启停正常，遇到错误能定位，已知限制有记录 |
| 4. 项目收尾 | 整理运行说明、设计说明、演示和项目讲解 | 他人能按说明复现，开发者能结合代码解释关键部分 |

第二阶段形成最小功能版本，第四阶段完成本规划。阶段推进以代码和证据为依据，不以“已经生成了代码”作为验收通过。

若范围超出可投入时间，优先减少行为类型和扩展功能，保留完整主流程。达到收尾标准后，可停止增加功能。

## 五、数据与验证标准

数据主要由自建测试容器产生，无需企业日志或大规模公共数据集。每个场景记录触发操作、预期行为、实际事件和判断结果。

| 场景 | 主要检查什么 |
| --- | --- |
| 应触发规则的行为 | 是否采集到事件，来源是否正确，告警依据是否一致 |
| 不应触发的对照行为 | 是否产生不合理告警，规则边界是否清楚 |
| 不同来源产生相似行为 | 是否混淆容器或进程，未知来源是否被错误归属 |
| 重复运行与异常情况 | 启停、容器变化、配置错误等是否影响核心流程 |

模拟事件和回放数据可以用于调试用户态逻辑，但最终采集验收要有目标环境中的真实运行证据。没有观察到事件，不能直接解释为行为没有发生。

测试发现的问题按实际情况记录，包括误报、漏报、来源识别失败及可能的事件缺失。不预设准确率、吞吐量和资源占用指标；需要展示数字时，保留对应环境和测试条件。

## 六、开发者与 Codex 的协作方式

- 开发者负责确定取舍、参与理解和验收；Codex 协助阅读源码、编写与修改代码、运行测试、排查问题和整理记录。
- 每轮集中处理一个可验证的小目标。Codex 先说明目标及依据，再实施并验证；常规可逆修改连续推进，无需每个小步骤都停下来确认。
- 关键功能完成后，简要说明数据从哪里来、经过哪些代码、为什么得到当前结果。通过小范围修改检查理解，避免要求一次读懂整个代码库。
- 涉及 Go 时，结合当前代码解释用到的语法、模块关系、错误处理和资源管理；使用并发时说明其必要性与退出方式，不要求先掌握完整语言体系。
- Codex 能访问目标环境时直接验证；不能访问时，完成可做部分，给出必要操作及预期结果，并将目标环境验证保留为待完成。
- 不凭空补写成功输出、截图、测试数字或实现状态。静态检查通过与实际运行通过分别记录。
- 使用项目已有目录、版本管理和文档约定。修改前检查已有代码，保留用户工作；新增依赖、扩大范围或明显增加成本时说明影响。

涉及内核和系统行为的判断，以对应版本资料、源码和实际结果相互核对。遇到不确定的问题，先缩小验证范围，不用未经验证的复杂方案掩盖问题。

## 七、进度与决策记录

本文保存相对稳定的目标。开发状态在项目现有进度文档中记录；没有现成记录时，再建立一份简短记录即可。

每次实际推进后记录：

| 项目 | 记录内容 |
| --- | --- |
| 阶段与改动 | 处理了什么问题，相关代码在哪里 |
| 实现与验证状态 | 区分未实现、已实现待验证、验证通过和存在阻碍 |
| 验证证据 | 环境、命令或操作、结果，以及日志或测试文件位置 |
| 未解决事项 | 哪些仍不确定，影响哪些功能或验收 |
| 已确定选择 | 采用的方案、理由和适用范围 |

重新开始协作时，Codex 先读本文、已有进度记录和实际代码，再判断从何处继续。没有证据的能力不自动标记为完成。

## 八、最终交付与结束标准

- [ ] 最小完成版本的全部必需能力已经实现并在目标环境验证。
- [ ] 正常、可疑和必要异常场景有可复现操作与实际结果。
- [ ] 代码、运行说明、简要设计、已知限制和演示材料可供查看。
- [ ] 开发者能解释采集机制、C 与 Go 的分工、Go 主程序中的事件处理流程、容器关联、规则判断和一次实际排错过程。
- [ ] 简历描述只包含实际完成的能力，使用的测试数字能够追溯。

文档数量和代码规模不作为完成指标。满足以上标准，即可按个人项目收尾。
