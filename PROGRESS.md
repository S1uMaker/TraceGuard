# TraceGuard 进度记录

最后更新：**2026-09-21**  
当前源码：**v0.2.0，轻量完善已实现，本轮未编译／测试**  
已验证基线：**v0.1.0，采集 → 容器归属 → 规则 → 记录的完整流程已通过 Ubuntu 首轮真实验收。**

2026-09-20 按最初要求交付源码，没有构建或执行测试。2026-09-21 首轮验收时，用户授权连接 Ubuntu 测试，并在自己的终端完成 sudo 认证；历史验收结果来自真实执行，详细证据见 [验证报告](docs/validation/2026-09-21/REPORT.md)。同日随后按“参考开源项目完善、暂时不需要测试”的要求交付 v0.2.0 增量，详情见第 7 节；没有再次连接虚拟机或覆盖此前证据。

## 1. v0.1.0 基线已实现与已验证

| 能力 | 实现位置 | 本次验证 |
| --- | --- | --- |
| 成功 exec 采集、固定 ABI、ring buffer | `bpf/exec.bpf.c` | C 编译、真实内核加载、成功事件与失败 exec 对照通过 |
| Go 加载/附加、304 字节解码、资源释放 | `internal/collector/` | 解码单测、真实采集、两次正常停止通过 |
| Docker 元数据与 cgroup inode 关联 | `internal/container/` | 双容器区分、宿主 PID 对照、新容器刷新通过；缓存边界有单测 |
| 严格 JSON 配置 | `internal/config/`、`configs/` | 合法配置、重复/未知字段、错误 scope、UID 等单测通过 |
| 两条默认规则与命中依据 | `internal/rules/` | shell/id 告警、sleep 不告警；条件组合与匹配边界单测通过 |
| JSONL、权限与独占锁 | `internal/output/` | 追加、权限拒绝、初始化失败释放锁、同目录第二实例拒绝通过 |
| run/doctor/check-config/replay | `cmd/traceguard/` | 环境检查、真实运行、退出重启和回放通过 |
| 合成回放 | `examples/` | 3 条事件、1 条 shell 告警，均核对 replay 标记 |
| 可重复验收 | `scripts/`、各包 `*_test.go` | 22 个顶层单元测试函数及子场景、竞态检测、静态检查、16 项集成检查通过 |

## 2. 当前设计取舍

- 首版仅采集成功 exec，不采失败尝试、文件访问或网络行为。
- 运行核心为 C/eBPF + Go；新增 Python 标准库脚本用于自动验收，不参与采集运行链路。
- 固定 tracepoint 布局并在启动前检查，不宣称 CO-RE 或任意内核兼容。
- 固定 `cilium/ebpf v0.17.3`、`x/sys v0.30.0`，已保留 Ubuntu 实际生成的 `go.sum`。
- 目标为单机、cgroup v2、rootful Docker；本次实际覆盖 amd64。arm64 仍未实测。
- 默认每 5 秒刷新容器信息，历史映射最多保留 5 分钟；无法确认的来源明确保留 unknown。
- 告警说明满足规则条件，不能直接判断攻击；持久化采用 JSONL，不引入数据库或 Web 页面。

## 3. v0.1.0 首轮验收改动与排查

1. 新增七个 Go 测试文件、完整验证入口 `scripts/validate_vm.sh` 和真实集成脚本 `scripts/verify_vm.py`；保留依赖校验和。
2. 首次真实验收中，失败 exec 的错误文本由本机 Docker 输出到 stdout；脚本只查 stderr，导致一项误判。实际命令返回 127，未出现该文件的成功 exec 事件。现同时核对两个输出流，并保留失败返回值、零对应事件和后续成功事件三个断言。
3. 输出权限测试的临时目录在本机 Go 环境下并非 0700；补充测试夹具显式设置权限后，再验证目标文件的权限错误。生产代码正确拒绝了原先不安全的目录。
4. 最终完整重跑全部通过；本轮没有为通过测试而修改监控核心或默认规则。同步修正文档中的“待验证”状态。

首版源码静态检查时修正过回放与实时目录冲突、同 inode 输入输出、空文件名回放、第二次退出信号、日志锁清理和非 Linux 占位实现；这些属于 2026-09-20 的交付历史。

## 4. 实际证据

| 运行 | 结果 | 位置 |
| --- | --- | --- |
| 首次完整验收 | 集成 15/16；失败项为测试脚本的报错输出流判断 | `build/validation/20260921T061521Z-6104/` |
| 补充单测与静态检查 | 修正权限测试夹具后全部通过 | 虚拟机 `build/userspace-final/`，亦在证据压缩包中 |
| 最终完整验收 | `exit_code=0`、`phase=completed`；22 个单测函数及子场景、race、vet、回放、16/16 集成通过 | `build/validation/20260921T062733Z-16549/` |
| 可随项目保留的结果 | 两次原始记录、最终摘要及报告 | [docs/validation/2026-09-21/](docs/validation/2026-09-21/REPORT.md) |

环境：Ubuntu 24.04.3 LTS、Linux 7.0.0-31-generic、x86_64、Go 1.22.2、Clang 18.1.3、Docker 29.1.3、cgroup v2/systemd。项目位于虚拟机 `/home/urgot/TraceGuard`。本次仅安装缺少的构建依赖，保留原 Docker 安装；构建产物与 Ubuntu 镜像留在虚拟机供后续演示复用。

## 5. 收尾和下一步

- [x] 记录实际环境、镜像摘要与产物摘要。
- [x] 构建、单测/竞态检测、静态检查、配置、回放和真实采集验收。
- [x] 保存原始失败证据、排查原因和最终通过记录。
- [x] 清理本次创建的三个测试容器及采集进程；最终核查无残留。
- [x] 撤销临时 SSH 公钥授权，确认新连接被拒绝；宿主机两组临时密钥共四个文件均已删除。
- [ ] 用户沿一条真实告警讲清数据流，亲自演示，再整理简历与面试表述。
- [ ] 待用户安排下一轮验证时，单独覆盖 v0.2.0 规则例外、统计及 JSON/text 输出；本轮不执行。
- [ ] 如有后续需求，再考虑性能压测、退出排空或容器生命周期订阅。

## 6. 已知限制

只对上述虚拟机与本次场景完成验收。最终完整验收的主采集收到并保存 86 条事件，其中 26 条来源 unknown；目标容器和宿主对照均独立核验，未把所有后台事件当作已归属。两次采集的 `kernel_dropped=0` 仅说明本次没有 ring buffer 预留失败，不能推导全局零漏报。

极短寿命容器可能错过刷新窗口；退出容器的历史映射不等于当前存活状态。首次 Docker 快照与容器退出竞争可能中止启动，运行中刷新失败会记录并降级。文件名匹配有相对路径、截断、改名、脚本和 BusyBox 的边界。停止时不排空缓冲，JSONL 无跨文件事务、逐条 fsync 或轮转。尚无吞吐量、准确率、长时间运行和故障注入数据。

## 7. v0.2.0 开源参考后的轻量完善（2026-09-21）

本轮授权范围：在首轮测试基础上完善个人项目，并同步 Markdown 文档，暂不测试。没有安装依赖、构建、执行单测／竞态检测／vet／回放／配置命令、启动 Docker、加载 eBPF 或连接虚拟机；保留现有测试文件和验证证据。

| 改动 | 实现与用途 | 本轮状态 |
| --- | --- | --- |
| 逐规则容器例外 | `exclude_container_names` 精确匹配已确认 Docker 容器；满足全部正向条件后只排除当前规则告警，原始事件继续保存；配置严格校验，`Match` 接口保留 | 源码完成，未测试 |
| 告警及例外分类统计 | `alerts_by_rule`、`excluded_alerts`、`excluded_by_rule`，区分实际保存的告警与规则例外；按本次进程累计 | 源码完成，未测试 |
| unknown 原因分类 | `unknown_reasons` 复用已有来源原因；固定分类及 `unspecified`/`other`，避免任意回放文字导致无限分类 | 源码完成，未测试 |
| 可选文本告警 | `alert_format` 配置及 run/replay 的 `--alert-format text`，单行显示关键字段和事件 ID；默认仍为 JSON，磁盘记录保留完整 JSONL | 源码完成，未测试 |
| 文档与版本 | CLI 版本更新到 0.2.0，更新 README、配置、设计、演示、项目规划、样例说明；新增开源参考说明 | 已记录 |

开源借鉴：保留 cilium/ebpf 作为既有依赖；借鉴 Falco 的逐规则例外设计、libbpf-bootstrap 的简洁终端展示。unknown 分类是本项目现有数据的延伸。没有复制外部源文件、增加依赖或更改内核探针，来源及取舍见 [docs/OPEN_SOURCE.md](docs/OPEN_SOURCE.md)。

本轮完成 Go 源码格式化、静态阅读与文档一致性核对；这些工作不等同于编译或测试通过。默认规则及默认配置保持原样，新版旧配置仍以 JSON 输出且不启用例外；这属于代码兼容设计，尚未重新运行验证。现有虚拟机二进制仍是此前产物，使用新功能前需同步源码并在 Ubuntu 重新构建。
